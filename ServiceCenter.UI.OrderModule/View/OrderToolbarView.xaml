<UserControl x:Class="ServiceCenter.UI.OrderModule.View.OrderToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="Height" Value="20"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,2,0"/>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <Button Content="Add" Command="{Binding AddClick}"/>
        <Button Content="Edit" Command="{Binding EditClick}"/>
        <Button Content="Delete" Command="{Binding DeleteClick}"/>
    </StackPanel>
</UserControl>
